export const metadata = {
    title: "Profile | Next Application",
    description: "Next Application",
  }
const profilePage = () => {
    return (
        <div>
            profile page is here
        </div>
    );
};

export default profilePage;